<link rel="stylesheet" href="/css/comments.css">

<li class="bord" id="comment-<%= comment._id %>">
   
        <div class="loc">
            <small id="na">
                <%= comment.user.name %>
            </small>
            <% if (locals.user && locals.user.id==comment.user.id){ %>
                <small>
                    <a class="delete-comment-button" href="/comments/destroy/<%= comment.id %>">delete</a>
                </small>
                <% } %>
               
            </div>
         <p><%= comment.content %></p>
               
   
</li>
